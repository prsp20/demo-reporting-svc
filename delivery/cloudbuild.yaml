steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - gcloud
      - functions
      - deploy
      - demo-reporting-svc
      - --region=FUNCTION_REGION
      - --source=.
      - --pull-request-trigger